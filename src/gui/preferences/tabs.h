#include <QVBoxLayout>
#include <QWidget>
#include <QScrollArea>

class Tabs : QWidget {
public:
    Tabs(QWidget* parent);
    
private:
    // each tab holds one of these sets of values
    // key bindings
    // colors(gates, wires, etc)
    // etc 
    

}; 
